import 'package:flutter/material.dart';

class ViewMedappointments extends StatelessWidget {
  const ViewMedappointments({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}