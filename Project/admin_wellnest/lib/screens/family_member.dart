import 'package:flutter/material.dart';

class FamilyMember extends StatelessWidget {
  const FamilyMember({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Family member"));
  }
}