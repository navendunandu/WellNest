import 'package:flutter/material.dart';

class ManageRoom extends StatelessWidget {
  const ManageRoom({super.key});

  @override
  Widget build(BuildContext context) {
    return  Center(child: Text("Manage Room"),);
  }
}