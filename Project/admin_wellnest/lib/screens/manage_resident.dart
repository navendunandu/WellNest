import 'package:flutter/material.dart';
class ManageResident extends StatelessWidget {
  const ManageResident({super.key});
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Resident"));
  }
}